RewriteEngine On

# Se o arquivo/pasta existir, serve direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Caso contrário, envia tudo pro front-controller
RewriteRule ^ index.php [QSA,L]
